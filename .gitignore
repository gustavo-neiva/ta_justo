# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore key files for decrypting credentials and more.
/config/*.key

# pi-loop lock file
.pi-loop.json.lock

.ratchet/
.ratchet.conf

# Local agent/tool dirs — config lives in AGENTS.md, not in the tree
/.claude/
/.gitnexus/

# Local-only CEASA ops scripts — machine-specific (BR egress + server creds);
# kept out of the public repo. Fetch runs locally per the geoblock note in AGENTS.md.
/bin/ceasa_local_fetch.sh
/bin/ceasa_sync_hetzner.sh
